<UserControl x:Class="OutputScreen.DiagnosticsView.DiagnosticsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:diagnosticsView="clr-namespace:OutputScreen.DiagnosticsView"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid d:DataContext="{d:DesignInstance diagnosticsView:DiagnosticsViewModel}">
            <StackPanel>
                <TextBlock Text="{Binding EffectorNext}" />
            <TextBlock Text="{Binding EffectorCurrent}" />
            <TextBlock Text="{Binding EffectorPrevious}" />
            <TextBlock Text="{Binding SensorPredicted}" />
            <TextBlock Text="{Binding SensorCurrent}" />
            <TextBlock Text="{Binding SensorPrevious}" />
        </StackPanel>
    </Grid>
</UserControl>
