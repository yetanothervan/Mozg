<UserControl x:Class="SomeToolbar.RegionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:someToolbar="clr-namespace:SomeToolbar"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <WrapPanel d:DataContext="{d:DesignInstance someToolbar:RegionViewModel}">
        <Button MinHeight="24" MinWidth="64" Command="{Binding DoStepCommand}">Шаг</Button>
    </WrapPanel>
</UserControl>
